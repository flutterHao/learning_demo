import 'package:flutter/material.dart';

class LifePage extends StatelessWidget{
   @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar:AppBar(
         title:Text('LifePage')
         ),
         body:Center(child: Text('this is LifePage'),)
    );
  }
}